// Global Settings

enum Shortcut
{
    Disabled,
    Hold,
    Toggle
}

// Graphics Settings

enum RenderMode
{
    Default,
    Limited,
    LimitedWithPilot
}

enum RenderType
{
    Default,
    OnlyEffects,
    Minimal
}

enum ShadowType
{
    Default,
    ParallelSplitShadowMaps
}

// Camera Settings

enum FieldOfView
{
    Default,
    Simple,
    Advanced
}

enum QuickZoom
{
    Disabled,
    Simple,
    Advanced
}